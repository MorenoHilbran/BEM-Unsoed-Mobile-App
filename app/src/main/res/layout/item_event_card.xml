<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Event Cover Image -->
        <ImageView
            android:id="@+id/iv_event_cover"
            android:layout_width="match_parent"
            android:layout_height="160dp"
            android:scaleType="centerCrop"
            android:src="@drawable/test1"
            android:contentDescription="@string/event_cover_desc" />

        <!-- Event Info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Event Title -->
            <TextView
                android:id="@+id/tv_event_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Workshop Mobile Development"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#1C1C1E"
                android:maxLines="2"
                android:ellipsize="end"
                tools:text="Workshop Mobile Development" />

            <!-- Event Date -->
            <TextView
                android:id="@+id/tv_event_date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="15 Oktober 2025, 14:00"
                android:textSize="14sp"
                android:textColor="#8E8E93"
                android:layout_marginTop="4dp"
                android:drawableStart="@drawable/ic_calendar"
                android:drawablePadding="8dp"
                android:gravity="center_vertical"
                tools:text="15 Oktober 2025, 14:00" />

            <!-- Event Location -->
            <TextView
                android:id="@+id/tv_event_location"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Gedung Rektorat Unsoed"
                android:textSize="14sp"
                android:textColor="#8E8E93"
                android:layout_marginTop="4dp"
                android:drawableStart="@drawable/ic_location"
                android:drawablePadding="8dp"
                android:gravity="center_vertical"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="Gedung Rektorat Unsoed" />

            <!-- Event Price & Organizer -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tv_event_price"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Gratis"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="@color/primary_color"
                    tools:text="Gratis" />

                <TextView
                    android:id="@+id/tv_event_organizer"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="by BEM Unsoed"
                    android:textSize="12sp"
                    android:textColor="#8E8E93"
                    tools:text="by BEM Unsoed" />

            </LinearLayout>

            <!-- Action Buttons -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="12dp"
                android:gravity="end">

                <Button
                    android:id="@+id/btn_remind"
                    android:layout_width="wrap_content"
                    android:layout_height="32dp"
                    android:text="@string/event_remind"
                    android:textSize="12sp"
                    android:textColor="#8E8E93"
                    android:background="@drawable/event_button_outline"
                    android:paddingHorizontal="12dp"
                    android:layout_marginEnd="8dp" />

                <Button
                    android:id="@+id/btn_register"
                    android:layout_width="wrap_content"
                    android:layout_height="32dp"
                    android:text="@string/event_register"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:textColor="@android:color/white"
                    android:background="@drawable/event_button_filled"
                    android:paddingHorizontal="16dp" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
